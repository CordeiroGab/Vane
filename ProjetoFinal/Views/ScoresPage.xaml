<Page
    x:Class="ProjetoFinal.Views.ScoresPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Background>
        <ImageBrush ImageSource="ms-appx:///Assets/ArquivoJogo/fundo.jpg"
                    Stretch="UniformToFill"/>
    </Page.Background>

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock Text="Scores" FontSize="72" FontWeight="Bold" Foreground="#FFFFFFFF" HorizontalAlignment="Center"/>
            <ListView x:Name="ScoresListView" Margin="0,20,0,0" HorizontalAlignment="Center">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Spacing="20">
                            <TextBlock Text="{Binding Nickname}" FontSize="24" Foreground="#FFFFFFFF"/>
                            <TextBlock Text="{Binding Score}" FontSize="24" Foreground="#FFFFFFFF"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <Button Content="Voltar" Click="BackButton_Click" HorizontalAlignment="Center" Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Page>
